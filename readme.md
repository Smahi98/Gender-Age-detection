# Gender & Age detection
This repository contains a model for locating faces and determining their ages and genders:

- The face detection model is named: `haarcascade_frontalface_alt.xml`   
- The age detection model is named: `age_net.caffemodel`  
- The gender detection model is named: `gender_net.caffemodel`  

## Installation
First clone this repository locally using:
```shell
git clone https://github.com/Smahi98/Gender-Age-detection
```

This project uses Python3 as a programming language, therefore it needs to be installed. Following that, the packages in `requirements.txt` file need to be installed as well:
```python
pip install -r requirements.txt
```

## Execution
To run the website, use the following command:

```shell
sh run.sh
```
